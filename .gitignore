# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# uv
# (uv.lock IS committed)

# Node / Next.js
node_modules/
web/.next/
web/out/
web/.turbo/
*.tsbuildinfo

# Env & secrets — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
xpub.txt

# Local dev
docker-compose.override.yml
data/
db-dumps/
*.log
.DS_Store
.idea/
.vscode/

# Build artifacts
dist/
build/
releases/

# Private / infra — kept local, NEVER published (describe the maintainer's own setup)
Aaron-*.md
deploy.sh
# exercises the host-local deploy.sh via --dry-run, so it stays host-local too
tests/deploy_resolve_test.sh
.rsync-exclude
docker-compose.override.yml.example
docker-compose.vps.yml
scripts/backup_db.sh
docs/DEPLOY.md
docs/11-operations.md
docs/private/